#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);

    int max=0,d;

    while(n>0)
    {
        d = n % 10;

        if(d > max)
            max = d;

        n = n / 10;
    }

    printf("%d",max);

    return 0;
}
